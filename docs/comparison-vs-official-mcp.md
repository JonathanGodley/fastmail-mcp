# This server vs. the official Fastmail MCP — which to use

> **What this is.** A comparison of two complete Fastmail MCP servers to answer one question: **which should you use?** "This server" = `JonathanGodley/fastmail-mcp` (v1.9.4-fork.7), the self-hosted MCP this repo builds — judged as a whole tool, not on the features that distinguish it from the upstream it was forked from (that lineage doesn't bear on which server to run).
>
> **Freshness header.** Probed **2026-06-25**. Official server: **`Fastmail` v1.0.0**, **29 tools**, protocol `2025-06-18`, remote streamable-HTTP/SSE. This server: **v1.9.4-fork.7** (commit `9f12d35`), local stdio, 40 tools. **Re-probe trigger:** this snapshot is valid as of official v1.0.0 / this server v1.9.4-fork.7 — re-run the audit if either advances, since tool counts, schemas, and token weights can all move.
>
> This is a **dated competitive snapshot**, not a timeless design model. It is deliberately distinct from the `docs/*.md` rationale files (`email-bodies.md`, `security-model.md`, `conventions.md`), which capture durable platform/codebase facts. Don't treat the numbers here as permanent or "fix" this file to match those — it ages by design and should be re-derived, not maintained.
>
> **What has since been corrected in place, and what has not.** The re-derivation this file asks for is open as **#106**, which carries the running list of everything here that has been falsified since the probe date. Three claims were corrected in the body anyway, because they described *this server's current behaviour* in the present tense and had become plainly untrue. In each case the correction strikes the false claim and marks the row **unsettled**; it does not re-score anything, because neither side has been re-probed:
> - **Serialization.** This server no longer pretty-prints; every result payload went compact in 2026-08 (#40). The Appendix measurements are unchanged and still record the pretty-printed "before" - see the note above §A2.
> - **Calendar recurrence.** `list_calendar_events` no longer lists only sparse series masters. See §3 for what was measured and when, note that this one is still moving, and note also what did *not* change: the listed rows still carry core fields only, with no `organizer`/`participants`.
> - **Thread bodies.** `get_thread` gained `includeBodies` in **#74**, so "this server needs `get_thread` + `get_email`×N to read a conversation" is false. It was stated in four places (§1, §2, §A2 and the §A3 matrix row); all four are struck. The §5 **P2** row is a separate matter and is deliberately left unmarked - see #106 for why marking it shipped would assert more than has been checked.
>
> - **Compose surface.** The three compose tools this snapshot names (`create_draft`, `reply_email`, `forward_email`) no longer exist. Composing is one `draft_email` tool with a `mode` of `new`/`reply`/`forward`, and the caller places this server's generated blocks by writing `{{signature}}`/`{{quote}}`/`{{forward}}` into the body rather than being given them automatically. Wherever those three names appear below, read them as the 2026-06-25 surface; the inventory and the matrix are NOT rewritten, per this file's own re-derive-don't-maintain rule. The one present-tense claim struck for it is the §5 **P3** row's closing sentence, which said the official's unified-compose ergonomics remained open here. Nothing is re-scored — neither side has been re-probed.
>
> Everything else on #106's list - the tool inventory, the compose-surface prose, contacts-write, `forward`, labels - is **still uncorrected here** and is waiting on the re-probe. Read the rest of this file as a 2026-06-25 snapshot.
>
> **Evidence grades used below.** Every comparative claim is tagged:
> - **probe-backed** — measured live, read-only, this session (token weights, id-equality, parity smoke).
> - **code-backed** — read from this repo's source (`src/index.ts`, `src/email-formatter.ts`).
> - **schema-inferred (black-box)** — read from the official server's published tool schema/description only; no source, not executed (every official *mutating* tool falls here — we never called one).
>
> **Measurement basis (so the magnitude isn't overread).** Token figures use a local `gpt-tokenizer` o200k_base tokenizer (v2.9.0) — a **GPT-family tokenizer used as a proxy**; the host model (Claude) tokenizes differently, *especially* whitespace, so **raw bytes are the primary, tokenizer-independent evidence** and every directional claim below also holds in bytes. The corpus is one account, one snapshot: a 25-email list page (24 survived the cross-server join), 3 single emails, 1 thread, and a calendar window. Treat directions as solid and exact magnitudes as indicative.
>
> All example payloads in this document are **fabricated**. No real account data, tokens, or addresses appear here.

---

## 1. Which should you use?

This compares the two servers as **complete tools** — this server judged on everything it does, not on the features that distinguish it from the upstream it was forked from (that lineage is irrelevant to "which should I use"). Tokens are a footnote here, not the verdict (see the note at the end of this section).

**Short answer: there is no forced winner — they optimize for different things. If you'd rather not maintain anything and want the official's broader feature set (Notes, contacts-write, forward, RSVP) now, the official is the better choice. If you value running and controlling your own tooling, and need its exclusives (local download, raw JMAP, structured search, the full draft lifecycle), the self-hosted server is the better fit — with the official as a complement to pull features from or run alongside.** It's a genuine tradeoff; here are the axes.

Both are complete Fastmail MCP servers covering the core job: read/search mail, drafts and replies, send, contacts, calendar. They differ on what surrounds that core.

**Where this server is the better tool to use:**
- **It's self-hosted and yours to change.** Local, in-process, fully under the operator's control, and extensible to anything public JMAP allows. The official is a fixed remote product you can't change or see into — any gap this server has can *likely* be closed in code where public JMAP reaches it (some may not — Notes in particular may be un-portable, §5); the official's gaps you cannot close at all.
- **Attachment download to local disk** (with path confinement). The official has *no* attachment-download tool at all — it uploads but never pulls a file down (schema-inferred).
- **Raw JMAP access** (`raw:true`) and **structured search** (`search_emails`' dedicated from/to/cc/bcc/subject/date/mailbox fields). The official is Gmail-string-only and never exposes the underlying JMAP.
- **A richer draft/compose workflow** — create, *edit*, and send-a-saved-draft, plus sanitized reply quoting and a draft-only reply (sending is a separate `send_draft` call). The official can create a draft but cannot edit it or send a saved one at all (§3). (It does have two compose advantages of its own: `forward`, which this server lacks, and a markdown body that's easier to drive.)
- **You depend on Fastmail's mature JMAP API, not a new product layer.** Both servers go through Fastmail — neither keeps your data local, and every call is a network round-trip either way. The difference is *which* Fastmail surface you rely on: this server rides **JMAP**, Fastmail's long-standing foundational API (the same one their own clients use), while the official rides their **brand-new MCP product** (v1.0.0), which can be rescoped, rate-limited, or changed as a product decision you can't inspect — and the tool/transformation layer in between is code you control rather than a black box.

**Where the official is the better tool to use:**
- **Broader out of the box:** Notes, memos, contacts write, forward, RSVP, calendar invitations (iMIP) — things this server doesn't do yet (schema-inferred).
- **First-party maintained:** it improves on Fastmail's effort, not yours, and won't bit-rot if you stop touching it.
- **Leaner default read output**, carrying slightly more status detail per message (probe-backed).
- ~~**Richer reads on threads**~~ *(no longer holds as stated; both halves of this bullet have been overtaken)*: `read_thread` returns the conversation's bodies in one call, and at the probe date this server needed a `get_email`×N fan-out to match it. It does not any more - `get_thread` gained `includeBodies` (with `stripQuoted`) in #74, so a whole conversation's bodies come back in one call here too. Whether the delivered shape matches the official's quote-stripped output has not been re-probed, so read this as "the fan-out claim is false", not as "this server now wins the row". *(Calendar recurrence was the other half of this bullet when it was written - "this server lists sparse masters" - and no longer holds either: `list_calendar_events` expands recurrences as of 2026-08. What is and is not settled there is in §3.)*
- **Interactive widget UX** (compose/confirm dialogs) if your MCP client renders them.

**An honest scoreboard, so §1 doesn't flatter the home team.** On the *shared* capabilities — where both servers try to do the same job — the **official wins more head-to-head rows** (read-thread, calendar-read, labels, archive, compose-body, string-search; several probe-backed — see the Appendix matrix). This server's case for being the better tool to use therefore rests **less on out-executing the official where they overlap** and **more on** (a) capabilities the official lacks entirely — local attachment download, raw JMAP, `search_emails`' structured field composition, and the full create→edit→send draft lifecycle — and (b) the meta-advantage of being yours to control and extend. Put plainly: if your needs sit inside the shared core, the official is at least as good and often richer; this server pulls ahead precisely where you need one of its exclusives or its malleability. **The two standout keep-reasons today are the faithful draft lifecycle (create→edit→send, which the official can't do — §3) and local attachment handling (download to disk, which the official has no tool for at all).** Those rest on this server's raw-JMAP access and so can't be reproduced by a layer over the official; the official's wins, by contrast (richer thread/calendar reads, the breadth tools), *can* be adopted — either built here or, better for the expensive ones, forwarded via the federated surface (§5, Option 4). If you keep developing this server, the natural split is to hold the draft + attachment core native and pull the rest from the official rather than reinvent it.

**The deciding axis is control + fit vs. breadth + zero-maintenance** — not tokens:
- If this server already does your core mail work and you value owning and shaping your tooling, **it's the better one to use**, and anything it lacks can be added in code (or obtained by running the official alongside).
- If you'd rather not maintain anything and want the official's exclusive features immediately, **the official is the better one to use** — accepting the loss of local download, raw JMAP, structured search, and control.
- **Running both** is a legitimate third answer: yours for local/raw/structured/download, the official for its exclusives. The cost is two overlapping tool sets and a search-dialect gotcha (§5).

One caveat before any switch is *final*, **and it cuts both ways**: this audit was read-only, so the official server's **mutating** side (send/draft/contacts-write/calendar) is inferred from its schemas, never executed — and so is *this* server's (its `edit_draft`/`send_draft`/`bulk_*` behaviour is read from this repo's source this session, not run). Neither server's write path was executed here, so every "X wins on a mutating capability" — in *either* direction — is a code/schema claim, not an observation. A consented write-probe is the honest last check before relying on the official for the things your MCP currently handles.

> **On tokens - a footnote, not the verdict.** This server was built to trim response tokens, and that is its headline difference *from the upstream it forked* - but it is not the axis on which to choose between it and the official server, and an earlier draft of this report wrongly treated it as such. The honest state of the measurement **as of the 2026-06-25 probe**: **this server was heavier, not lighter** (≈322 vs 257 tokens/message; 9,605 vs 6,375 tokens for a 25-mail page) - its founding metric *lost* to the official. Compacting the serialization (information-preserving) closed much of the as-served part of that gap and **has since been done**, in 2026-08; the per-message figure is unaffected, because it was already measured on a compact re-serialization of both sides. Going further to ≈204 tokens/message would mean dropping some fields from the *default* view, which is a separate per-field product decision (those fields are real data - see §4), not a given. Either way the gap is small enough that token weight shouldn't pick the server: decide on capability + control. Numbers in §Appendix A2.

Capability detail behind this verdict is §3–§4; the feature-by-feature matrix is the Appendix.

---

## 2. Decision factors beyond features

- **First-party longevity / maintenance** *(judgment, not a probed finding — but a heavy one).* Official = Fastmail-maintained (first-party, v1.0.0). This server = a single-maintainer codebase (derived from `MadLlama25/fastmail-mcp`). No public release-cadence signal was established this session, so **cadence is not establishable** here — but the bus-factor asymmetry is stark: a company's product vs. one maintainer. The honest trade: **developing this server means one person maintains it against a first-party product that keeps adding features.** One counterweight, in fairness: the official side carries its *own* continuity risk — a first-party remote product can be deprecated, rescoped, rate-limited, or have its token scope changed unilaterally, and you control none of that. So the asymmetry is "one volunteer maintainer vs. one vendor's product roadmap," not strictly risky-vs-safe.
- **Auth / deployment / transport.** Official: remote HTTPS, a **scoped Bearer access token** (a long-lived token you mint with a specific scope — *not* an interactive OAuth flow, so there is no refresh/expiry dance), streamable-HTTP/SSE — nothing to run locally, but nothing runs *without* the network and the host must speak remote-HTTP MCP. This server: local stdio (`node dist/index.js`), long-lived API token, runs in-process next to the host. **Local-filesystem attachment confinement (`FASTMAIL_ATTACH_DIR`) is only meaningful for a local server; it is structurally impossible remotely** (code-backed — the official server has no filesystem on your machine to confine to, and no download tool at all).
- **Host-support precondition (unverified).** Switching to *or* adding the official server assumes your MCP host can natively mount a remote Bearer-auth HTTP server. Today the host runs this server as a stdio `command: node` launcher. The probe proves the **endpoint** is reachable from a script; it does **not** prove the **host** can wire to it. One config question, not a known blocker — but price an adapter/bridge into any migration estimate until confirmed.
- **Host-capability dependency (widget UX).** The official server advertises an `extensions` capability and ships MCP-UI widgets (`compose_event`, `confirm-delete-event/note/contact`, an `email-view`). On a host that renders MCP-UI these are a real UX win; on a plain stdio CLI host they degrade to "staged, awaiting a confirm the host can't show." A **host-capability dependency**, not a portable feature — and whether it matters at all depends on your client.

---

## 3. What each side does better

**This server does better:**
- **`raw:true` escape hatch on every email tool** (code-backed) — full canonical JMAP on demand. The official server has **no raw mode**; what it returns is what you get. This matters whenever simplification drops a field you actually needed.
- **Local attachment download + path confinement** (code-backed) — `get_email_attachments` + `download_attachment` (to disk, confined to `FASTMAIL_ATTACH_DIR` when set). The official server **cannot download an attachment at all** (schema-inferred — upload only).
- **`search_emails` structured fields** (code-backed) — multi-criteria JMAP-level search you compose from **fields** (from/to/cc/bcc/subject/date/mailbox, alongside the free-text query), no query-string to build. (This was a separate `advanced_search` tool; it is now consolidated into `search_emails`.) The official `search_email` is a **Gmail-style string** (schema-confirmed) — and it's *not* qualifier-poor: its schema documents `from to cc bcc with subject body text in before after is has` plus extensions (`me`, plain-name contact lookup, `in:` by id/name/role, relative dates `7d`/`1w`). So this server's edge here is **programmatic/structured composition** (machine-friendly, no string parsing), *not* a larger searchable surface — the two are different input models for comparable expressiveness.
- **Explicit bulk-by-id tools** (code-backed) — `bulk_move`/`bulk_pin`/`bulk_mark_read`/`bulk_delete`/`bulk_add_labels`/`bulk_remove_labels`. The official server batches via `ids[]` on `update_email`/`archive_email`/`delete_email` (schema-inferred) but has no single-call "pin these 30" / "move these to mailbox X".
- **Full draft lifecycle** (code-backed) — `create_draft` → `edit_draft` (partial edits, `clearFields`, attachment add/remove, faithful recreate that preserves threading headers + keywords and sends the replaced draft to Trash rather than destroying it) → `send_draft`. The official can only *create* a draft (`draft_email`); it has **no edit-draft and no send-saved-draft tool**, and its `update_email` schema exposes no body/recipient fields (schema-inferred — not executed; a runtime probe could in principle reveal undeclared field acceptance, which is why the §1 write-probe caveat applies) — so any compose-iteratively-then-send flow is this-server-only on the published surface. *(Symmetric caveat: this server's own draft-edit/send behaviour is code-backed — read from source this session, not executed — so this row is two unexecuted write paths compared, not an observed head-to-head.)*
- **Reply quoting + draft-only compose** (code-backed) — `reply_email` sanitizes the quoted HTML (strips scripts/handlers, keeps formatting), attributes and top-posts to match the web client, and **always saves a draft** rather than transmitting (sending is a separate `send_draft` call). Plus lenient recipient input (comma-separated strings, `"Name <email>"`, bool-or-string) vs the official's structured `[{name,email}]`.
- **Runs in the user's host today** (code-backed) — stdio, already wired, no transport question.

**Official does better:**
- **Token efficiency on the core read path *as measured on 2026-06-25*** (probe-backed) - this was the official's one *measured* edge, and it was real: its default output was leaner than this server's simplified output for list, single-email, and thread (numbers in §Appendix). The gap had two parts, and **only one of them has since closed**: this server's pretty-printing went away in 2026-08 (§5 P0), so the whitespace component is no longer paid; the extra-fields component remains, and moving those fields behind `raw:true` is still undecided (§5 P1). The two servers have **not been re-measured against each other since**, so the current head-to-head number is unknown - what can be said is that this server was heavier on both counts at the probe date and has since removed one of them. Weigh it as a modest point in the official's favour, not as decisive.
- ~~**Reads thread bodies in one call**~~ (probe-backed **2026-06-25**; *the fan-out half no longer holds*) — `read_thread` returns quote-stripped bodies inline, and at the probe date this server's `get_thread` returned metadata only, forcing a `get_thread` + `get_email`×N fan-out to read a conversation. `get_thread` gained `includeBodies` (and `stripQuoted`) in #74, so the fan-out is gone. The two shapes have not been compared since, so the row is unsettled rather than flipped. (What was claimed here was a round-trip/capability difference, not the single-thread token number, which is illustrative only; see §Appendix.)
- **Expanded recurring calendar events, and returned richer event detail inline** *(probe-backed **2026-06-25**; this server's half has since changed - read the whole bullet before scoring it)* - over the same window, `search_events` returned each *occurrence* of a recurring event as its own dated row (e.g. a weekly session appeared 7× with a `recurrenceId`), so "what's on next week" was directly answerable; at that date this server's `list_calendar_events` returned one row per event object with **no occurrence expansion** and a sparse field set (`start`/`end`/`title`/`id`/`url`), pushing both the recurrence math and the detail (participants, location, duration, timezone) onto a per-event `get_calendar_event` fan-out. The official's event rows also carry that detail inline.

  **What changed on this side (#64, 2026-08).** `list_calendar_events` now asks the CalDAV server to expand a recurring series, filters the result to the window the caller asked for, distinguishes an expanded occurrence (`recurrenceId`, dated at the real occurrence) from a series master (`recurrenceRule`), reports a `total` so a `limit`-trimmed page is visible (#100), and marks a bound the caller left open with the range it actually searched. So "no occurrence expansion" no longer describes it.

  **"Sparse field set" still does, and deliberately.** The listed rows do *not* carry `organizer` or `participants`: only `get_calendar_event` passes `includeParticipants`, and README states the rule plainly - `list_calendar_events` returns core fields only, no participants, for token efficiency. `location` and `description` were on the listed rows before #64 as well, so neither is something that change added. The per-event fan-out for attendee detail is therefore intact, and this row keeps that part of the official's edge.

  **That is not the same as the gap being closed, and this row should not be re-scored on it.** Expansion was still being corrected as this paragraph was written: the first cut dropped every occurrence of a series whose original `DTSTART` fell inside the queried window, because Cyrus omits `RECURRENCE-ID` on a series' *first* instance and a parser reading "the block with no `RECURRENCE-ID`" as the master discarded its siblings (measured in `scripts/probes/calendar-expand.probe.mjs`). Nothing here has been re-probed head-to-head against the official server since 2026-06-25, and the official's calendar surface has had its own time to move. Treat this row as **unsettled pending a re-probe of both sides**, not as flipped.
- **Strips quoted reply history on read by default** (official schema-inferred; this server code-backed) — `read_email` takes `includeQuotedText` (default false), returning just the new message and dropping the quoted chain unless asked; this server's `get_email` always returns the full `bodyText`. The three sampled messages carried no quoted chain (their bodies were byte-identical across both servers), so they cannot themselves probe this; that the edge "only bites on long reply chains" is reasoned from the feature's definition, not measured here — but for a reply-heavy mailbox it is a real cleanliness/cost edge.
- **Notes** (schema-inferred) — `search_notes`/`read_note` (+ write tools). This server has **zero** notes support.
- **Memos** (schema-inferred) — private per-email annotations via `set_memo`, surfaced inline in `read_email`/`search_email`. This server has none — this is both a write gap *and* a read gap.
- **Contacts write** (schema-inferred) — `create_contact`/`update_contact`/`delete_contact`. This server's contacts are **read-only** (list/get/search only).
- **Calendar invitations / RSVP** (schema-inferred) — `create_event`/`update_event` explicitly send iMIP to attendees, and `rsvp_event` responds to invitations. This server can attach participants to an event (code-backed) but does not surface iMIP-sending or any RSVP path.
- **Forward** (schema-inferred) — `draft_email`/`send_email` support `mode: 'forward'`. This server has reply but **no forward**.
- **Unified, markdown compose** (schema-inferred) — one `draft_email`/`send_email` pair driven by `mode: new|reply|forward`, with a **markdown `body`** auto-converted to HTML (+ a plain-text alternative) and `replyAll`. Ergonomically simpler for a caller than this server's separate compose tools and explicit `htmlBody`/`textBody` (which has no markdown helper — formatted mail means writing HTML yourself). A cleaner *interface*, even though this server's draft *lifecycle* is richer.
- **Gmail-style search string** (schema-confirmed) — friendlier for ad-hoc natural queries than building structured criteria, with a rich qualifier set + extensions (relative dates `7d`/`1w`, plain-name contact lookup, `me`), and it **surfaces any attached memo inline** in the results. A usability edge, not a capability this server lacks an answer to.
- **Richer label model** (probe-backed) — `list_labels` returns id/path/role/parentId/counts; this server's `list_mailboxes` is lighter but carries less structure.
- **Trajectory** *(judgment)* — the capability gaps below are a snapshot, not a fixed list. A first-party v1.0.0 that already matches this server's body-trimming and ships Notes/memos/contacts-write out of the gate will keep adding; the port shortlist is a gap that **re-opens** as the official server iterates.

---

## 4. Gaps each way

**This server only (official lacks entirely):** attachment **download to disk** (the official has no download tool — though it *does* surface attachment metadata inline on read, so attachment *listing* is shared); local-filesystem **confinement**; `raw:true` full-JMAP; `search_emails`' structured field composition; **draft editing** (`edit_draft`) and **sending a saved draft** (`send_draft`) — the official can create a draft but, per its schema, never modify or later-send it (schema-inferred — its `update_email` exposes no body/recipient fields, and was not executed this session); `get_account_summary`; explicit bulk-by-id pin/move.

**Official-only (this server lacks entirely):** **Notes** (read + write); **memos** (read + write); **contacts write**; **forward**; **RSVP** to invitations; **iMIP-sending** calendar invites (surfaced as such); **Gmail-style** search dialect; interactive **widget UX**. (A dedicated `archive_email` was on this list; it has since shipped here.)

**Load-bearing asymmetry (the headline, framed honestly):** at the probe date this server's *simplified* output was **heavier** than the official's default - but this is a tradeoff, not pure waste. Two components: (a) this server **pretty-printed** (~17% pure-whitespace overhead by bytes, no information - a clean, free loss), and (b) this server **returns more fields** in its default view (`blobId`, `listUnsubscribe`, `references`, `inReplyTo`, `keywords`, `replyTo`, `size`). Component (b) is *paid-for content*, not inefficiency: whether those fields belong in the default view is a **product judgment** (a list reader rarely needs threading headers or a blob id), not a measured defect. **Component (a) has since been taken: serialization went compact in 2026-08 (§5 P0), so that ~17% is no longer paid.** Component (b) is untouched - moving those fields behind `raw:true` would take this server's per-message weight *below* the official's (204 vs 257 tok; these per-message means are over the 24-of-25 cross-server message join, so treat them as indicative - see A1), and remains an open per-field decision. Most of that projected lead is **information-preserving**: this server omits empty `cc`/`bcc` and false flags that the official emits regardless (omit-empties - same information, fewer tokens). The trimmed view genuinely carries *less* on only two axes - no `isAnswered`/`isForwarded`, and name-only `mailboxes` vs structured `labels` (§Appendix). Bottom line: this server measured **heavier on both components** at the probe date, has since removed the free one, could be made **modestly lighter still at near-equal information** by a decision not yet made, and either way **token weight is not a meaningful differentiator**.

---

## 5. Recommendation paths + port shortlist

Four ways to go (§1), all defensible depending on what you value:
- **Develop this server** — keep using it and add what you want. The shortlist below mixes items already **done** (P0, P4 — marked ✅ in the table, and no longer open calls) with *candidates* that are not decided: P2 is cheap and low-stakes, P1 is an open per-field decision (it changes the default output), P3's scope has since grown (forward shipped natively, so what remains is the official's unified-compose *mode* ergonomics, not the feature), and the expensive ones are where you should weigh "build it" against "just get it from the official server."
- **Switch to the official** — if you'd rather not maintain anything and want its exclusive features now. Gate a *final* switch behind a consented mutation probe (its write side is schema-inferred, not tested) and the host-wiring check (§2).
- **Run both** — this server for download/`raw`/structured `search_emails`/bulk, the official for its exclusives (Notes/memos/contacts-write/forward/RSVP). **Cost:** two overlapping tool sets and a **search-dialect-confusion hazard** — this server's `search_emails` takes free-text plus JMAP-backed structured fields while the official `search_email` takes a Gmail-style string, so with both loaded a wrong-dialect query fails silently or misleads. Loading this server **on-demand** (only when you need a download) sidesteps that cost, since the two are never live at once.
- **Federate into one surface (a solid middle ground)** - keep this server as the single front door: it serves its own strengths **natively** (drafts, attachment download, `raw:true`, structured `search_emails`, bulk) and, instead of *reimplementing* the official's exclusives, **forwards those calls to the official MCP** and re-exports their tools as its own. You get the first-party Notes/memos/contacts-write/RSVP with **no business logic to build or maintain**, while keeping the faithful native draft + attachment paths (no lossy round-trip through the official's plaintext-only read). MCP-server-as-MCP-client is a standard pattern, and the official-side client already exists (the read-only HTTP/SSE client built for this audit). Auth is just a **second scoped Bearer access token** - the same kind already used here, long-lived, **no OAuth refresh dance**. **Curate to avoid overlap:** proxy only the exclusives (and optionally the official-better reads - forward, one-call thread bodies; calendar-recurrence was on this list and has since been built natively instead, §3), keep everything you do as-well natively - which *removes* the Run-both search-dialect hazard, since each capability is exposed exactly once. **Caveats:** the interactive widget tools (`compose_event` / confirm-delete) likely won't survive a stdio proxy hop - skip them; proxied official *mutators* belong behind a deliberate allowlist (same posture as this audit's read-only gate); and one surface now mixes local-fast and remote tools, so a remote outage degrades the proxied half. **Bonus:** a local stdio server bridging to the official's HTTP endpoint *is* the adapter the §2 host-support precondition asks for - so this doubles as the way to use the official from a stdio-only host. Net effect: it converts the shortlist's most expensive items (Notes / memos / contacts-write) from "build" to "forward."

**Port shortlist (for the develop path).** Value = coarse high/med/low of how often the gap bites a daily Fastmail user *(a judgment — override freely)*. JMAP-reachability is **inferred, not probe-verified** (the audit ran no mutators): "likely" = reuses machinery this server already has; "Verify" = depends on a mechanism not yet confirmed. Un-portable items are dropped (they're reasons to run the official alongside). Below the P2 line, "build it here" competes with "just use the official for that piece" — decide per feature. **Rows tagged 🔁 are strong Option-4 *proxy* candidates** — forwarding to the official's first-party implementation likely beats building and maintaining your own, *especially* the Verify-gated ones whose JMAP reachability is unconfirmed.

| # | Item | Value | Cost | JMAP-reachable? | Landing notes |
|---|------|-------|------|-----------------|---------------|
| **P0** ✅ | **Stop pretty-printing; serialize compact** | Info-preserving¹ | Trivial | n/a (local) | **Done.** Every payload site now serializes through one seam (`toolJson`/`redactedJson` in `src/coerce.ts`, neither of which takes an indent argument), with a drift guard in `src/tool-schema.test.ts`. Re-measured live afterwards: **4,856 bytes (17.32%)** off a 25-message `list_emails` page, 24.91% off the same page under `raw:true`, 28.51% off a `list_mailboxes` result — closely matching the ~17% projected here. Removes no information. Acts on the as-served payload, *not* the per-message compact figure, so it does not stack with P1's per-message drop. **The appendix tables below predate this change** and still show the pretty-printed figures. |
| **P1** | **Move some default fields behind `raw:true`?** | **Open per-field decision**¹ | Low | n/a (local) | `blobId`/`keywords`/`references`/`inReplyTo`/`replyTo`/`size`/`listUnsubscribe` are currently in the *default* `simplifyEmail` view. Moving them behind `raw:true` takes the compact per-message figure 322 → 204 tok / 562 B — **but this removes real data from the default output (a user-visible change), and several plausibly belong there** (`references`/`inReplyTo` threading, `listUnsubscribe`, `replyTo`). **Not decided — a per-field judgment.** Anything that moves must stay in the `raw:true` superset (JMAP-property-consistency rule, `CLAUDE.md`). |
| **P2** | **Inline thread bodies in `get_thread`** | High | Low | likely (already fetched) | Match `read_thread`: return quote-stripped bodies inline so reading a thread is one call, not an N+1 `get_email` fan-out. Reuse the existing quote-stripper (`reply-quote.ts`). Ranked above forward/archive because the fan-out bites every thread read. |
| **P3** ✅ | **Forward** (`mode: 'forward'`) | Med | Med | shipped | **Done, as a tool rather than a mode.** `forward_email` reproduces the original under a forwarded-message block, carries its attachments *and* the images its body displayed, and offers the lossless `asAttachment` (`message/rfc822`) form. The attachment-forwarding wrinkle budgeted for here is what most of that work turned out to be. ~~What remains open from this row is only the official's *unified-compose ergonomics* (one tool with a `mode`), which is tracked separately as the surface-ergonomics audit — not the forward capability itself.~~ *(No longer holds: composing is now one `draft_email` tool with a `mode`, so the ergonomics half of this row is closed too. The row is left unsettled rather than re-scored — see the freshness header.)* |
| **P4** ✅ | **Archive** (`archive_email`) | Med | Low | shipped | **Done.** Note that the approach sketched here was wrong: reusing the `move_email` plumbing means a whole-value `mailboxIds` replace, which destroys every other folder and label the message was filed under. What Fastmail actually does is patch the Inbox membership away and keep the rest, adding Archive only when nothing else holds the message. The measurements are in `docs/fastmail-action-availability.md`. |
| **P5** 🔁 | **Contacts write** (create/update/delete) | Med | Med | likely | New mutators over the existing Contacts read client; delta semantics for emails/phones like the official's add/remove model. **Or proxy (Option 4)** — first-party write logic for free. |
| **P6** 🔁 | **Memos** (read + write) | Med | Med | **Verify** | Official surfaces memo text inline on read *and* via `set_memo`. Confirm whether memos are a public-JMAP keyword/mechanism vs. a Fastmail-private store. If reachable, it's also a **read-side** enrichment (show memo on `get_email`), not just a write tool. **Strong proxy candidate (Option 4)** — if it's a Fastmail-private store, proxying is the *only* way to get it. |
| **P7** 🔁 | **RSVP** (`rsvp_event`) | Low–Med | Med | Verify (CalDAV/iMIP) | Respond to invitations. This server's calendar is CalDAV-based; confirm the iMIP reply path is reachable before committing. **Or proxy (Option 4)** rather than re-derive the iMIP path. |
| **P8** 🔁 | **Notes** (read + write) | Low–Med | High | **Verify** | New `Note/*` object type → new client surface + simplifier rows + tests (the expensive kind: a whole new entity). Gate entirely on whether public JMAP exposes a Note type. **Best proxy candidate (Option 4)** — forwarding to the official's Notes suite skips this whole build, and if Notes turns out *not* to be public-JMAP-reachable, proxying is the only way to have them without running a second server. |

¹ **P0 and P1 are different in kind, and neither is about "winning" on tokens** (token weight is not a differentiator - §1). **P0** (compact serialization) was **information-preserving** - it drops only whitespace, so it was low-stakes and reversible; it was taken in 2026-08 and is no longer an open call. **P1 is NOT free**: it would **remove real fields from the default output**, which is a user-visible contract change and a per-field product decision - *explicitly not decided here*. Some of those fields may well belong in the default. Listed top of the shortlist only because they're *cheap to implement*, not because either is a settled action.

**Dropped as un-portable (no public-JMAP analog to *build*):** the **widget UX** (host-capability dependency). Note that "un-portable" now means "can't be *rebuilt* on JMAP" — it no longer means "must run a second server," because **Option 4 lets you forward to the official's implementation instead of porting it.** The widget tools are the one thing Option 4 also can't cleanly deliver (they likely won't survive a stdio proxy hop), so genuinely-interactive widget UX remains the only capability that truly requires the official server running in a widget-capable host. If, after verification, **Notes** turns out not to be public-JMAP-reachable, that is now a reason to *proxy* them (Option 4), not necessarily to run two separate servers.

---

## Appendix

### A1. Probe method (abstract)
Read-only only, both servers, behind a single frozen-allowlist choke-point (default-deny; ignores the server's `readOnlyHint`, since four official tools self-report read-only while staging mutations). Official reached via a purpose-built HTTP/SSE client (token from env, no body/header logging); this server reached via its stdio harness wrapped in the same allowlist. Token counts use a local `gpt-tokenizer` o200k_base tokenizer (v2.9.0) — a **GPT-family proxy**, since the host model's tokenizer is not public; **raw bytes are co-reported as the tokenizer-independent measure and every directional claim holds in bytes too**. Per-message figures normalize over the **common Message-ID set** (for the 24 messages present on *both* servers' pages, cross-server JMAP ids were equal — 0 mismatches — so a direct id join is valid; the 1 non-joiner is simply absent from one side's page, not an id-mismatch). That 1 of 25 is *attributed* to **page-boundary differences** (each server's "25 most recent" set is not the identical 25, due to ordering/inclusion at the edge) — a mechanism argument, not a measured no-skew check; with 24 of 25 retained, treat the per-message averages as indicative rather than precise. Note the two A2 tables use different bases: the as-served page is **25 messages plus the envelope**, while per-message averages are over the **24-message common set**, so the two do not multiply directly (e.g. official 257×24 ≠ the 6,375 as-served figure). Information is held constant: an omitted field is a content delta, never counted as a token "win." The negative-control test confirmed the gate blocks all forbidden tools pre-request (8/8 official incl. all four widget liars, 8/8 this server) with zero requests built.

### A2. Token weights (probe-backed, 2026-06-25; o200k_base proxy + bytes)

> **These tables record the "before".** Every figure here was measured on 2026-06-25, when this server pretty-printed its payloads; it went compact in 2026-08 (§5 P0), so the "pretty-printed" rows below are history, not current behaviour. They are left as measured rather than restated, because the compact figures in the P0 row come from a **different, later account snapshot** and mixing the two would produce a number nobody measured. The per-message table is unaffected either way - it was already normalized on a compact re-serialization of both sides.

**Email list page (25 recent), as-served (what the host receives):**

| Representation | tokens | bytes | serialization |
|---|---|---|---|
| this server (simplified) | 9,605 | 24,962 | pretty-printed |
| this server (`raw:true`) | 11,481 | 31,494 | pretty-printed |
| **official** | **6,375** | **18,338** | compact |

The as-served gap (this server 9,605 → official 6,375 tok; in bytes 24,962 → 18,338) has two parts:
- **Pretty-print + envelope whitespace** *(this component was removed in 2026-08 - §5 P0; the figures below are what it cost while it lasted)* - re-serializing the *identical* content compactly gives 9,605 → 8,031 tok and 24,962 → 20,675 bytes, i.e. **~4,287 bytes (~17% of the as-served pretty-printed payload) / ~1,574 tokens (~16%)** of pure whitespace, zero information. **The byte figure is the reliable one**: whitespace tokenization is exactly where a GPT-family proxy and the host model diverge most, so trust the ~17% byte reduction over the ~16% token count here. This part loses zero information (it's only whitespace), but it does **not** by itself close the per-message gap (compact-whole this server 8,031 still > official 6,375).
- **Remaining difference** = this server carrying more fields (next table).

**Per-message normalized (compact re-serialization, common set n=24):**

| | tokens/msg | bytes/msg | field richness |
|---|---|---|---|
| this server (as shipped) | 322 | 830 | its field set + 7 extra |
| **official** | **257** | **737** | official field set |
| this server (**projected**: 7 extra fields moved behind `raw:true`) | **204** | **562** | near-equal info (see below) |

The projected 204/562 is a **direct measurement** (re-serialize each common-set message with exactly the seven fields removed, then count - removing a field also removes its key + delimiters, so the realized saving exceeds the field-value subtotal below). Crucially, most of this server's resulting lead is **information-preserving**, not "carrying less": this server **omits empty/false fields** (`addIf` drops empty arrays; `addFlag` drops `isFlagged`/`isDraft`/`isReply`/`hasAttachment` when false - `email-formatter.ts`), so on these mostly-no-`cc`, unflagged sample messages this server spends no tokens on `cc`/`bcc`/flags while the official emits them anyway (e.g. `cc:null`). That is a genuine efficiency at equal information. This server carries *less* on only two points: it surfaces no `isAnswered`/`isForwarded` at all, and its `mailboxes` are name-only vs the official's structured `labels`. So this server's projected view is **modestly lighter at near-equal information** - not a strict-superset win, but not "lighter by dropping content" either. Combined with the as-shipped reality at the probe date (this server heavier on both components), the fair takeaway is **token weight is not a meaningful differentiator**; pretty-print removal was the only unambiguous free gain and has since been taken (§5 P0), and omit-empties is a real but modest edge for this server.

**Per-field token *value* drivers over the common 24 (indicative only).** These are field values tokenized in isolation; o200k_base merges across boundaries, so per-field token counts are **non-additive and do not sum to the per-message totals** — read them as a directional decomposition, and lean on bytes. (A per-field structural/key breakdown is omitted deliberately: it could only be a back-solved residual, which is not a measurement.)

| field | this server (value tok) | official (value tok) |
|---|---|---|
| preview | 1,534 | 1,534 *(identical — info held constant)* |
| messageId | 781 | 781 |
| from | 317 | 490 *(official structured `{name,email}`)* |
| to | 278 | 425 |
| labels / mailboxes | 88 *(names)* | 566 *(full `{id,name,path,role}`)* |
| blobId / references / inReplyTo / replyTo / keywords / size / listUnsubscribe | 568 / 388 / 261 / 245 / 334 / 46 / 708 | — *(official omits all seven)* |
| status flags (isAnswered/isFlagged/isForwarded/isDraft) | ~8 | ~96 *(official carries the full set)* |

Reading: this server's net heaviness is the seven extra content fields it returns; the official is heavier on structured `from`/`to`/`labels` *and* on always emitting empty `cc`/`bcc`/status-flags that this server omits. Neither is a strict superset, so there is no single clean iso-information number — but the omit-empties difference favors this server at equal information (see the projected-row note above).

**Single email (`get_email` vs `read_email`), n=3 — per-sample observations, not a stable rate:**

| sample | this server `get_email` | official `read_email` | this server `raw:true` | body chars (this server / official) |
|---|---|---|---|---|
| #0 | 756 | 432 | 2,525 | 543 / 543 |
| #1 | 970 | 652 | 2,879 | 1,447 / 1,447 |
| #2 | 505 | 308 | 928 | 472 / 472 |

Three samples spanning a ~3× size range; the official is lighter in each, by the same *kind* of metadata overhead seen in the list — though the per-sample delta varies (324 / 318 / 197 tok), so it isn't a single constant — and n=3 cannot establish a stable per-email rate, so read these as individual observations. Both return **byte-identical** plain-text bodies (HTML omitted on both sides; these samples carried no quoted reply chain, so the official's default quote-stripping made no difference here), so the gap is metadata, not body content. A separate targeted probe confirms the official's read path is **text-only even for richly-HTML mail**: on a message this server reports as ~57 KB of HTML, `read_email` returned a plain-text rendering with **no HTML markup in any field and no `blobId`** — so unlike this server (whose `raw:true` recovers the true `htmlBody`), the official exposes **no path to a message's HTML source at all**. That's why a *federated read* off the official (§5, Option 4) could never recover formatting — it's a structural limit of their read surface, not a sampling artifact. `raw:true` (full JMAP incl. HTML) is ~2–3× heavier on these short-bodied samples — confirming simplification is a real win *over raw JMAP*, which the official server matches independently. (The earlier draft's larger multiples came from a different, HTML-heavy sample set; the multiple scales with how much HTML the message carries.)

**Thread (`get_thread` vs `read_thread`) - n=1, illustrative only:** this server 1,150 tok (4 members, **no bodies**); official 966 tok (5 members, **with** quote-stripped bodies). (Byte counts were not separately captured for this single thread; with n=1 and the caveat below, the token figure is illustrative only and no directional claim rests on it.) This server being *heavier* while carrying *fewer* members and *no* bodies is the same per-member metadata + pretty-print overhead measured in the list section (the pretty-print half of which has since gone - §5 P0), not a thread-specific effect - and this thread had short bodies, so **do not generalize the token number** (a thread with long un-quotable bodies could invert it). The capability difference this recorded - the official reads a thread's content in one call, this server needs `get_thread` + `get_email`×N - **no longer holds**: `get_thread` gained `includeBodies` (with `stripQuoted`) in #74. The measurement above was taken before that and is a reading of the metadata-only shape, which is still what a default `get_thread` returns. The member-count gap (4 vs 5) is **not a bug**: this server's `getThread(..., includeDrafts = false)` deliberately excludes draft messages (the missing member was a trashed draft), while the official includes them - a behavioral difference that is defensible but **can surprise** a user who expects the web client's view: a thread made *entirely* of trashed drafts comes back from this server with **0 members** while the official returns them. Worth knowing, and arguably an argument for a future `includeDrafts` toggle.

### A3. Full capability matrix

Direction key (F = this server, O = official): **F>O** this-server-superset · **O>F** official-superset · **≈** mixed tradeoff · **=** parity · **⊘** disjoint.

**Evidence (F·O) per side** — the confidence behind *each* server's half of the row, weaker side governs how hard the verdict can lean: **P** probe-backed (executed read-only this session) > **C** code-backed (read in this repo's source, not executed) > **S** schema-inferred (official's published schema, black-box, never executed) > **—** absent. Every official *mutating* tool is **S** (we never called one), so any `O>F` resting on official write behaviour is a schema claim, not an observation; rows graded `C·S` or `—·S` carry that caveat. The asymmetry cuts both ways: it is **not** always the official side that is weaker (e.g. label *read* is `P` on the official side and the this-server write tools are `C`). A `=` or `≈` on a row graded `C·S` (both sides a write tool) means **schema/code-equivalent with neither side executed** — a defensible parity claim, not an observed runtime match.

| Capability | This server's tool(s) | Official tool(s) | Dir | Evidence F·O |
|---|---|---|---|---|
| List/recent emails | `list_emails`, `get_recent_emails` | `search_email` (no query) | ≈ *(functionally equal; official's only edge is lighter as-shipped output — the token weight §1 calls non-decisive)* | P·P |
| Search emails (string + structured) | `search_emails` (free-text query AND dedicated from/to/cc/bcc/subject/date/mailbox fields; Trash/Spam excluded by default) | `search_email` (Gmail-style string, qualifier-rich + memo-inline) | ≈ *(official's string dialect is friendlier; this server adds programmatic field composition + default Trash/Spam scoping. One consolidated tool now, not two)* | C·S |
| Read single email | `get_email` (+`raw`) | `read_email` (+`includeQuotedText`) | ≈ *(official lighter + strips quoted history by default; this server has `raw:true` full-JMAP)* | P·P |
| Read thread | `get_thread` (metadata; bodies under `includeBodies`, #74) | `read_thread` (bodies) | **unsettled** *(scored **O>F** on 2026-06-25 on a `get_email`×N fan-out that #74 removed; the two body shapes have not been compared since)* | P *(2026-06, superseded)*·P *(2026-06)* |
| Full-JMAP escape hatch | `raw:true` (all email tools) | — | **F>O** | C·— |
| List mailboxes/labels | `list_mailboxes`, `get_mailbox_stats` | `list_labels` | O>F *(richer label model)* | P·P |
| Account summary | `get_account_summary` | — | **F>O** | C·— |
| Send / reply | `send_email`, `reply_email` | `send_email` (`mode`) | ≈ *(different models — this server: sanitized HTML quoting + draft-only reply (`send_draft` transmits) + lenient input; official: markdown body + `replyAll` + unified `mode`)* | C·S |
| Forward | — | `send_email`/`draft_email` `mode:forward` | **O>F** | —·S |
| Compose body format | explicit `htmlBody`/`textBody` (HTML-preferred, auto text fallback) | markdown `body` → HTML + text alt | O>F *(markdown is easier for a caller; this server needs raw HTML for formatting)* | C·S |
| Draft lifecycle | `create_draft` → `edit_draft` → `send_draft` | `draft_email` *(create only)* | **F>O** *(official has no edit-draft and no send-saved-draft; `update_email` can't touch body/recipients)* | C·S |
| Mark read / pin / flag | `mark_email_read`, `pin_email` | `update_email` | ≈ *(capability-equal; this server's edge is lenient id/bool input coercion, official's is one parameterized call — neither decisive)* | C·S |
| Move / label | `move_email`, `add_labels`, `remove_labels` | `update_email` | ≈ *(this server uses dedicated verbs + lenient id coercion, official folds into `update_email`; same outcomes, mild ergonomic differences)* | C·S |
| Archive (dedicated) | `archive_email` | `archive_email` | ≈ *(both dedicated; this server's is measured against the client's own semantics — see `docs/fastmail-action-availability.md` — and takes `emailIds` as an array)* | C·S |
| Delete (trash) | `delete_email` | `delete_email` | = | C·S |
| Bulk by id | `bulk_mark_read`, `bulk_pin`, `bulk_move`, `bulk_delete`, `bulk_add_labels`, `bulk_remove_labels` *(this row covers all six)* | `ids[]` on update/archive/delete | F>O *(pin/move-by-mailbox)* | C·S |
| Bulk self-test (meta) | `test_bulk_operations` | — | F>O *(this-server orphan; meta/test tool, classification-only — not invoked in this audit)* | C·— |
| Read attachments | `get_email_attachments` *(dedicated list tool)* | attachment metadata inline on `read_email` | ≈ *(this server has a dedicated tool; official folds it into read)* | C·S |
| Download attachment to disk | `download_attachment` (+confinement) | — | **F>O** *(the true exclusive — official has no download tool)* | C·— |
| Upload attachment | via `attachments` param *(inline on compose/draft/reply only)* | `upload_attachment` *(standalone)* | ≈ *(official's standalone tool enables upload-once-reference-by-id across messages; this server uploads inline per compose call — same single-message outcome, different lifecycle)* | C·S |
| Contacts read | `list_contacts`, `get_contact`, `search_contacts` | `search_contacts` | = *(both returned empty for the probe query)* | P·P |
| Contacts write | — | `create_/update_/delete_contact` | **O>F** | —·S |
| Identities | `list_identities` | `list_identities` | = | P·P |
| Calendars list | `list_calendars` | `list_calendars` | = | P·P |
| Events read | `list_calendar_events`, `get_calendar_event` | `search_events` | **unsettled** *(scored **O>F** on 2026-06-25, when this server listed sparse masters with no occurrence expansion. It expands recurrences as of #64, 2026-08, but the listed rows still carry core fields only - no `organizer`/`participants`, so the attendee fan-out remains. That work was also still being corrected and neither side has been re-probed, so the direction is not re-derivable from here; §3)* | P *(2026-06, superseded)*·P *(2026-06)* |
| Events write | `create_/update_/delete_calendar_event` (participants) | `create_/update_/delete_event` (+iMIP) | O>F *(iMIP surfaced)* | C·S |
| Event compose widget | — | `compose_event` | O>F *(host-dependent)* | —·S |
| RSVP | — | `rsvp_event` | **O>F** | —·S |
| Memos | — *(this server has none)* | `set_memo` (+inline on read) | **O>F** | —·S |
| Notes | — | `search_/read_/create_/update_/add_to_/delete_note` | **O>F** *(read side `search_notes` probed live — returned empty, account has none)* | —·P/S |
| Capability probe (meta) | `check_function_availability` | — | F>O *(meta)* | C·— |

Orphan rows (no counterpart) are listed with a "—" rather than silently dropped: this-server-orphans = `search_emails`'s structured fields (free-text + from/to/cc/bcc/subject/date/mailbox in one tool), `get_account_summary`, `download_attachment`, `test_bulk_operations`, `check_function_availability`; official-orphans = the Notes suite, memos, contacts-write, forward, RSVP, `compose_event`. (`get_email_attachments` is **not** an orphan — the official surfaces the same attachment metadata inline on `read_email`, so it sits in the shared "read attachments" row; only *download to disk* is a true this-server exclusive.)

**Tool counts:** this server 39, official 29. More tools ≠ more capability — this server's count includes 6 explicit `bulk_*` tools, two meta/test tools, and several read variants the official folds into fewer, more parameterized tools.

### A4. Full tool inventories (so the matrix's coverage is falsifiable)

Both lists were read from each server's live `tools/list` this session. Every name below appears in the §A3 matrix or its orphan list; cross-check freely.

**Official (29):** `add_to_note`, `archive_email`, `compose_event`, `create_contact`, `create_event`, `create_note`, `delete_contact`, `delete_email`, `delete_event`, `delete_note`, `draft_email`, `list_calendars`, `list_identities`, `list_labels`, `read_email`, `read_note`, `read_thread`, `rsvp_event`, `search_contacts`, `search_email`, `search_events`, `search_notes`, `send_email`, `set_memo`, `update_contact`, `update_email`, `update_event`, `update_note`, `upload_attachment`.

**This server (39):** `add_labels`, `bulk_add_labels`, `bulk_delete`, `bulk_mark_read`, `bulk_move`, `bulk_pin`, `bulk_remove_labels`, `check_function_availability`, `create_calendar_event`, `create_draft`, `delete_calendar_event`, `delete_email`, `download_attachment`, `edit_draft`, `get_account_summary`, `get_calendar_event`, `get_contact`, `get_email`, `get_email_attachments`, `get_mailbox_stats`, `get_recent_emails`, `get_thread`, `list_calendar_events`, `list_calendars`, `list_contacts`, `list_emails`, `list_identities`, `list_mailboxes`, `mark_email_read`, `move_email`, `pin_email`, `remove_labels`, `reply_email`, `search_contacts`, `search_emails` (consolidated: free-text + structured fields, was a separate `advanced_search`), `send_draft`, `send_email`, `test_bulk_operations`, `update_calendar_event`.
